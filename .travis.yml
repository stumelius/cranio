  language: python
  cache: pip
  python:
    - 3.6
  install:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start
    - pip install -r requirements.txt
    - pip install -e .[test]
  script:
    - pytest test